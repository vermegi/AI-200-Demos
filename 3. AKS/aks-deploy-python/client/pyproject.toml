[project]
name = "aks-client"
version = "0.1.0"
description = "Console client for AKS Foundry Gateway API"
requires-python = ">=3.9"

dependencies = [
    "httpx==0.25.1",
    "python-dotenv==1.0.0",
]

[tool.uv]
dev-dependencies = [
    "black==23.12.0",
    "flake8==6.1.0",
]
