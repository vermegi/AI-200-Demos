$repoRoot = Get-Location

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\aks-deploy-python.zip") `
    -DestinationPath (Join-Path $repoRoot "3. AKS\aks-deploy-python") `
    -Force

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\aks-configure-python.zip") `
    -DestinationPath (Join-Path $repoRoot "3. AKS\aks-configure-python") `
    -Force

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\aks-troubleshoot-python.zip") `
    -DestinationPath (Join-Path $repoRoot "3. AKS\aks-troubleshoot-python") `
    -Force
