$REDIS_HOST = $redisHost
$REDIS_PORT = $redisPort
$REDIS_DATABASE = $redisDatabase

$REDIS_HOST
$REDIS_PORT
$REDIS_DATABASE

# Set as environment variables so the Python app can connect to Azure Managed Redis.
$env:REDIS_HOST = $REDIS_HOST
$env:REDIS_PORT = $REDIS_PORT
$env:REDIS_DATABASE = $REDIS_DATABASE

# Configure the Python environment.
cd "C:\Users\gvermeiren\github\AI-200-Demos\6. Redis\amr-data-operations-python"

# python -m venv .venv
.\.venv\Scripts\Activate.ps1
# pip install -r requirements.txt

# Run the Redis data operations console app.
python main.py

deactivate