$repoRoot = Get-Location

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\postgresql-build-agent-python.zip") `
    -DestinationPath (Join-Path $repoRoot "5. PostGreSQL\postgresql-build-agent-python") `
    -Force

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\postgresql-vector-search-python.zip") `
    -DestinationPath (Join-Path $repoRoot "5. PostGreSQL\postgresql-vector-search-python") `
    -Force

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\postgresql-optimize-vector-search-python.zip") `
    -DestinationPath (Join-Path $repoRoot "5. PostGreSQL\postgresql-optimize-vector-search-python") `
    -Force
