$repoRoot = Get-Location

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\aca-deploy-python.zip") `
    -DestinationPath (Join-Path $repoRoot "2. ACA\aca-deploy-python") `
    -Force

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\aca-manage-python.zip") `
    -DestinationPath (Join-Path $repoRoot "2. ACA\aca-manage-python") `
    -Force

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\aca-scale-python.zip") `
    -DestinationPath (Join-Path $repoRoot "2. ACA\aca-scale-python") `
    -Force

