$repoRoot = Get-Location

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\cosmosdb-build-query-python.zip") `
    -DestinationPath (Join-Path $repoRoot "4. Cosmos\cosmosdb-build-query-python") `
    -Force

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\cosmosdb-implement-vector-python.zip") `
    -DestinationPath (Join-Path $repoRoot "4. Cosmos\cosmosdb-implement-vector-python") `
    -Force

Expand-Archive `
    -Path (Join-Path $repoRoot "mslearn-azure-ai\downloads\python\cosmosdb-optimize-query-python.zip") `
    -DestinationPath (Join-Path $repoRoot "4. Cosmos\cosmosdb-optimize-query-python") `
    -Force

