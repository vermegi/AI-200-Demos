apiVersion: v1
kind: ServiceAccount
metadata:
  name: index-client-sa
  namespace: default
  labels:
    app: index-client
  annotations:
    # Client ID of the identity with Cosmos DB Built-in Data Contributor access.
    azure.workload.identity/client-id: "MANAGED_IDENTITY_CLIENT_ID"